# Security

- Report issues privately to the repo owner. No bounty program.
- Python runtime is stdlib-only. Do not add `eval` of user JSON beyond `json.loads`.
- Do not ingest `main.dol` or Melee ISOs into this tree.
- Unreal plugin does not open raw sockets; netcode is a stub interface.
