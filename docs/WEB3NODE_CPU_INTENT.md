# web3node CPU intent

JuniorHome `web3node/ternary_tda.py` can emit roster logits
(Vesper / Quill / Relay / Forge).

BitNet / ternary scores intent only. `python/frameforge/math_kb.py` still owns knockback.
Do not import residual drift as launch scale.
